{
  "name": "immo-scanner",
  "version": "1.0.0",
  "description": "ImmoScanner — backend Playwright + proxy Claude API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "setup": "npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "playwright": "^1.44.0"
  }
}
